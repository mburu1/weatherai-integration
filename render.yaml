services:
  - type: web
    name: weatherai-integration
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: WeatherAI__ApiKey
        sync: false
    autoDeploy: true